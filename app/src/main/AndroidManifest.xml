<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.AnotherQuizApp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalApp">
        <activity android:name=".ResultsActivity" />
        <activity android:name=".ui.quizzes.QuestionsActivity" />
        <activity android:name=".ui.chapters.ChapterFiveActivity" />
        <activity android:name=".ui.chapters.ChapterSevenActivity" />
        <activity android:name=".ui.chapters.ChapterSixActivity" />
        <activity android:name=".ui.chapters.ChapterFourActivity" />
        <activity android:name=".ui.chapters.ChapterThreeActivity" />
        <activity android:name=".ui.chapters.ChapterTwoActivity" />
        <activity android:name=".ui.chapters.ChapterOneActivity" />
        <activity android:name=".ui.quizzes.QuizzesActivity" />
        <activity android:name=".ui.learn.LearnCPlusPlusActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.profile.EditProfileActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>